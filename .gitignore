secrets/
review/
*.pyc
__pycache__/
.env

# Real account config — never commit (contains customer IDs)
config.yaml

# Example config is safe to commit — no credentials, placeholder IDs only
!config.yaml.example
